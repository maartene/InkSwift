{
  "roadmap": {
    "project_id": "native-compiler-emission-alignment",
    "created_at": "2026-06-16T08:00:00Z",
    "total_steps": 4
  },
  "phases": [
    {
      "id": "01",
      "name": "TheIntercept real-fixing (probe-driven)",
      "description": "Pursue the TheIntercept e2e against the REAL story (playback probe as the gate), one real blocker at a time. Step 01-01 clears the line-4 dead-end at the real opts gather. Compiler/ only; no runtime change.",
      "default_agent": "nw-software-crafter",
      "default_deps_strategy": "sequential",
      "steps": [
        {
          "id": "01-01",
          "name": "Advance native TheIntercept past the real opts gather",
          "criteria": [
            "Native TheIntercept no longer dead-ends at the real opts gather; it plays strictly more than 4 oracle-matching lines, reaching at least the waited stitch (oracle index 4).",
            "A ratchet AT pins the achieved floor: native[0..<N] == oracle[0..<N] with N strictly greater than the prior 4.",
            "The real opts gather is handled: a labelled gather with a variable-text lead, multiple nested choices including level-2 choices, and a -> opts loop-back.",
            "No regression in the full suite (incl. the Phase-3 variable-text-threading and #4b choice-label ATs, both TheIntercept playthroughs)."
          ],
          "agent": "nw-software-crafter",
          "deps": [],
          "test_file": "Tests/SwiftInkRuntimeTests/Acceptance/Compiler_TheInterceptProgressTests.swift",
          "scenario_name": "TheIntercept native plays past the opts gather, matching the oracle prefix",
          "files_to_modify": [
            "Sources/SwiftInkRuntime/Compiler/Codegen/WeaveEmitter.swift",
            "Sources/SwiftInkRuntime/Compiler/Codegen/RuntimeObjectEmitter.swift",
            "Sources/SwiftInkRuntime/Compiler/Parser/InkParser.swift",
            "Tests/SwiftInkRuntimeTests/Acceptance/Compiler_TheInterceptProgressTests.swift"
          ],
          "implementation_notes": "TEST PARADIGM: example-based Swift Testing oracle execution-equivalence per CLAUDE.md (OOP; PBT N/A). Backtick function-name style MANDATORY; @Test(\"string\") FORBIDDEN. PROBE-DRIVEN REAL-FIXING (user decision 2026-06-16): synthetic minimal fixtures false-greened (see feature-delta #4b re-diagnosis), so drive against the REAL story. The driver/diagnostic is DIAG_INTERCEPT2 (Tests/.../Diagnostics/TheInterceptPlaybackProbe.swift): native currently dead-ends at output line 4 entering the real opts gather (start knot, source ~line 85): a LABELLED gather '- (opts)' with a variable-text lead {|I rattle...|}, nested choices (think)/(plan)/[Wait] where (plan) has its own level-2 choices (cooperate/dissemble/delay), and a '-> opts' loop-back; the gather closes '- -> waited'. Phase 3 fixed only a shallow miniature; this is the real shape. RED: author an ENABLED ratchet AT in a NEW suite Compiler_TheInterceptProgressTests.swift asserting native plays >=N oracle-matching lines (compile TheIntercept, play along [0,2,1,0,0,1,2,0,1,0], assert native.count>=N && native[0..<N]==oracle[0..<N]); set the RED target N just past the opts gather (>=5, the waited line). It must fail now (native has 4 lines). GREEN: fix the real opts emission in Compiler/ (do NOT change the runtime); after GREEN run DIAG_INTERCEPT2 and set N to the ACTUAL achieved matching-line count (maximize the ratchet up to the next real blocker). Investigate with a throwaway dump (remove before commit). If a fix needs a file beyond files_to_modify or a runtime change, STOP and escalate. Do NOT re-enable the TheIntercept e2e (full equivalence not yet reached). Boundary: no Engine/ import (R1); no JSONSerialization (R5); no runtime/Decoder change. DONE (commit 4686262): stitch-local divert qualification; native 4->9 played, matched floor N=6."
        },
        {
          "id": "01-02",
          "name": "Resolve the real deeply-nested start.delay read-count guard",
          "criteria": [
            "The real start.delay dotted read-count (a choice label nested 3 levels deep: start -> opts -> plan -> delay) resolves to .readCount, so the {not start.delay} guard on the tellme choice in start.waited evaluates correctly.",
            "harris_demands_component.cant_talk_right likewise resolves (same deep-nesting class).",
            "The DIAG_INTERCEPT2 surviving-dotted count drops toward 0 and native advances past oracle index 6; the ratchet AT N is bumped to the new achieved floor.",
            "No regression in the full suite."
          ],
          "agent": "nw-software-crafter",
          "deps": ["01-01"],
          "test_file": "Tests/SwiftInkRuntimeTests/Acceptance/Compiler_TheInterceptProgressTests.swift",
          "scenario_name": "native TheIntercept plays past the opts gather, matching the oracle prefix",
          "files_to_modify": [
            "Sources/SwiftInkRuntime/Compiler/Codegen/WeaveEmitter.swift",
            "Sources/SwiftInkRuntime/Compiler/Codegen/RuntimeObjectEmitter.swift",
            "Tests/SwiftInkRuntimeTests/Acceptance/Compiler_TheInterceptProgressTests.swift"
          ],
          "implementation_notes": "PROBE-DRIVEN. After 01-01 the probe's next blocker is oracle index 6: the {not start.delay} guard on the tellme choice in start.waited. start.delay is a surviving unresolved dotted variableReference: (delay) is a choice label nested 3 levels deep (start -> opts gather -> plan choice -> delay choice), referenced FLAT as start.delay. inklecate resolves by NAME in the knot's flat namespace regardless of physical depth; native's discovery pre-pass registers by physical path so the flat ref misses (the shallow #4b fixture passed but the deep real ref does not). FIX (Compiler/ only): extend the discovery pre-pass (WeaveEmitter) so a labelled weave point nested at ANY depth in a knot is ALSO addressable by knot.label (name in the knot's flat namespace), keyed to its already-computed absolute path (reuse keyPrefix; never re-derive); ensure lowerExpression resolves it (RuntimeObjectEmitter) and the auto-flag reconciliation flags the target. RED: bump the ratchet AT N to just past oracle index 6 (the tellme path). GREEN: fix; re-run DIAG_INTERCEPT2; set N to the actual achieved floor. Throwaway dump ok (remove before commit). If blocked beyond Compiler/ or needs a runtime change, STOP and escalate. Do NOT re-enable the TheIntercept e2e. Boundary R1/R3/R5; no runtime/Decoder change. DONE (commit 64aa56f): deep folded-label registration under flat knot-namespace; dotted refs 2->0; native matched 6->11."
        },
        {
          "id": "01-03",
          "name": "Advance native TheIntercept past the next blocker (probe-driven)",
          "criteria": [
            "Native advances past the current probe blocker at oracle index 11 (the -> pushes_cup divert from the [Deny] choice body, TheIntercept.ink ~line 121).",
            "The ratchet AT N is bumped to the new achieved oracle-matching floor (> 11).",
            "No regression in the full suite."
          ],
          "agent": "nw-software-crafter",
          "deps": ["01-02"],
          "test_file": "Tests/SwiftInkRuntimeTests/Acceptance/Compiler_TheInterceptProgressTests.swift",
          "scenario_name": "native TheIntercept plays past the opts gather, matching the oracle prefix",
          "files_to_modify": [
            "Sources/SwiftInkRuntime/Compiler/Codegen/WeaveEmitter.swift",
            "Sources/SwiftInkRuntime/Compiler/Codegen/RuntimeObjectEmitter.swift",
            "Sources/SwiftInkRuntime/Compiler/Parser/InkParser.swift",
            "Tests/SwiftInkRuntimeTests/Acceptance/Compiler_TheInterceptProgressTests.swift"
          ],
          "implementation_notes": "PROBE-DRIVEN (DIAG_INTERCEPT2). After 01-02 native matches 11 oracle lines then runs out at index 11: oracle continues 'Harris looks disapproving. He pushes one mug halfway towards me...' = the -> pushes_cup divert from the [Deny] choice body (TheIntercept.ink ~line 121). INVESTIGATE the real cause with a throwaway dump (remove before commit) — likely a divert-target qualification or choice-body-divert emission issue analogous to 01-01's stitch-local divert fix, but confirm against the real structure. FIX in Compiler/ only (no runtime change). RED: bump ratchet AT N past index 11. GREEN: fix; re-run DIAG_INTERCEPT2; set N to the actual achieved floor; full suite green. COMMIT with Step-Id + Task-Id trailers; in the body state achieved N + the next blocker. Prefer committing the COMMIT-phase log entry WITH the code (avoid a separate --no-verify chore commit). If blocked beyond Compiler/ or needs a runtime change, STOP and escalate. Do NOT re-enable the e2e. Boundary R1/R3/R5. DONE (commit 4d25dbf): deeper-gather parsing + sibling-body ordinal + weave-label divert + mid-line divert/glue; native matched 11->15."
        },
        {
          "id": "01-04",
          "name": "Advance native TheIntercept past the next blocker (probe-driven)",
          "criteria": [
            "Native advances past the current probe blocker at oracle index 15 (the post-lift_up_cup gather, TheIntercept.ink ~line 148).",
            "The ratchet AT N is bumped to the new achieved oracle-matching floor (> 15).",
            "No regression in the full suite."
          ],
          "agent": "nw-software-crafter",
          "deps": ["01-03"],
          "test_file": "Tests/SwiftInkRuntimeTests/Acceptance/Compiler_TheInterceptProgressTests.swift",
          "scenario_name": "native TheIntercept plays past the opts gather, matching the oracle prefix",
          "files_to_modify": [
            "Sources/SwiftInkRuntime/Compiler/Codegen/WeaveEmitter.swift",
            "Sources/SwiftInkRuntime/Compiler/Codegen/RuntimeObjectEmitter.swift",
            "Sources/SwiftInkRuntime/Compiler/Parser/InkParser.swift",
            "Tests/SwiftInkRuntimeTests/Acceptance/Compiler_TheInterceptProgressTests.swift"
          ],
          "implementation_notes": "PROBE-DRIVEN (DIAG_INTERCEPT2). After 01-03 native matches 15 oracle lines then runs out at index 15: oracle continues with the post-lift_up_cup gather (TheIntercept.ink ~line 148, 'Quite a difficult situation,' he begins, sternly...). INVESTIGATE the real cause with a throwaway dump (remove before commit). FIX in Compiler/ only (no runtime change). RED: bump ratchet AT N past index 15. GREEN: fix; re-run DIAG_INTERCEPT2; set N to the actual achieved floor; full suite green. COMMIT with Step-Id + Task-Id trailers (stage execution-log.json WITH the code commit; no separate --no-verify chore commit); in the body state achieved N + the next blocker. If blocked beyond Compiler/ or needs a runtime change, STOP and escalate. Do NOT re-enable the e2e. Boundary R1/R3/R5."
        }
      ]
    }
  ],
  "implementation_scope": {
    "source_directories": [
      "Sources/SwiftInkRuntime/Compiler/"
    ],
    "test_directories": [
      "Tests/SwiftInkRuntimeTests/"
    ],
    "excluded_patterns": []
  },
  "validation": {
    "status": "approved",
    "reviewer": "orchestrator (probe-driven phase; gates run before execution)",
    "approved_at": "2026-06-16",
    "notes": "Probe-driven real-fixing per user decision 2026-06-16 (minimal fixtures false-greened). Single step clears the line-4 real-opts-gather dead-end, gated by the DIAG_INTERCEPT2 playback probe + a ratchet AT. Further blockers handled in subsequent steps as the probe reveals them."
  }
}
